server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://auth-db:5432/nexus_auth_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect